<Page x:Class="WPF.Pages.ListLiterature"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ListLiterature">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <DataGrid x:Name="userDataGrid" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding name}"></DataGridTextColumn>
                <DataGridTextColumn Header="Author" Binding="{Binding author.author_name}"></DataGridTextColumn>
                <DataGridTextColumn Header="Public" Binding="{Binding date_public}"></DataGridTextColumn>
                <DataGridTextColumn Header="Description" Binding="{Binding description}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Image Grid.Column="1" Name="img"></Image>
    </Grid>
</Page>
